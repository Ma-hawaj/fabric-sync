-- The app's database. Authentik uses the server's default database
-- (POSTGRES_DB, i.e. `authentik`), created automatically by the image
-- entrypoint — only the app database needs creating here.
CREATE DATABASE fabric_sync;
